{
  "name": "omniremit-frontend",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=24",
    "pnpm": ">=9"
  },
  "scripts": {
    "dev": "pnpm --filter host dev",
    "build": "pnpm --filter host build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  }
}
