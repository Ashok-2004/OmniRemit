# packages/

Reserved for future shared workspace packages (e.g. a shared UI kit or federation-runtime helpers
used by both the host and later remote apps). Empty for now — the host app under `../apps/host` does
not yet depend on anything here.
